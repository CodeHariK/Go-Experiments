version: "2"
sql:
  - engine: "postgresql"
    queries: "store/schema/sql/"
    schema: "store/schema/schema.sql"
    gen:
      go:
        package: "query"
        out: "store/query"
        sql_package: "pgx/v5"
        emit_json_tags: true